select_content_and_date_from_entries = "SELECT content, date FROM entries;"

insert_into_entries = "INSERT INTO entries VALUES (?, ?);"

create_table_entries = "CREATE TABLE IF NOT EXISTS entries (content TEXT, date TEXT);"
